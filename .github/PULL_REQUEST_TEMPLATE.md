## What & why

<!-- What does this change and why? Link any related issue (Fixes #123). -->

## How I tested

<!-- e.g. ran `npm run check`; tested in EN + 中; demo + real send; mobile width. -->

- [ ] `npm run check` passes (JS parses)
- [ ] Tested in both languages (EN / 中)
- [ ] Tested the affected flow (template / recipients / import / SMTP drawer / send)
- [ ] Checked mobile width
- [ ] No new dependencies / build step added to the front-end
- [ ] Updated docs (`README.md` / `config.js` comments) if config or behaviour changed
