{
  "name": "codedrop-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Optional reference backend for CodeDrop — sends batches over SMTP.",
  "license": "MIT",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "nodemailer": "^6.9.0"
  },
  "engines": {
    "node": ">=18"
  }
}
